## What this changes

<!-- One or two sentences. -->

## How you checked it

<!-- For a ranking or alias change, paste the before/after: the question you
     asked, and the threads it returned each time. Claims get checked against
     real output before merge, so showing it here speeds things up. -->

```
before:
after:
```

## Checklist

- [ ] `uv run pytest -q` passes
- [ ] No captured Blind pages committed (fixtures are generated by `make_fixtures.py`)
- [ ] Any new request goes through `http.fetch`, not `httpx` directly
- [ ] robots/cache/throttle behaviour unchanged, or explained above
